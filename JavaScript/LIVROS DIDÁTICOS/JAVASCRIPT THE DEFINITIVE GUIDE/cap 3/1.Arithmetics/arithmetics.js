console.log(Math.pow(2, 2));              // it empowers a number by any number
console.log(Math.round(3.5));             // it rounds the number to the nearest integer, it can be above or below
console.log(Math.ceil(0.6));              // it round up the number to an integer
console.log(Math.floor(2.9));             // it round down the nubmer to an integer
console.log(Math.abs(-5));                // it gives me de aboslute value of a number
console.log(Math.max(2, 6, 4));           // it chooses the largest number
console.log(Math.min(2, 6, 4));           // it chooses the samllest number
console.log(Math.random());               // it gives me a random number (0 <= x < 1.0)
console.log(Math.PI);                     // it's the constant pi = 3.1415[...]
console.log(Math.E);                      // it's the natural logarithm = 2.7182[...]
console.log(Math.sqrt(25));                // it makes the square root of a number

console.log(Math.pow(27, 1/3));           /* doesn't exist other roots values in javascript, so,
                                          *  to calculate roots larger than 2 you'll need to use
                                          *  divisions as much as the example
                                          */ 
console.log(Math.cos(0));                 // trigonometry as much as: Math.cos(), Math.tan(), []...]
console.log(Math.log(10));                // natural logarithm of 10
console.log(Math.log(100)/Math.LN10);     // base 10 logarithm of 100
console.log(Math.log(512)/Math.LN2);      // base 2 logarithm of 512
console.log(Math.exp(3));                 // Math.E cubed or powered by any number

// everything in it's right place !!!